<nav class="navbar navbar-expand-lg topbar" aria-label="<%= aria_label %>">
  <% current_lang = I18n.locale.to_s.start_with?('fr') ? 'fr' : 'en' %>
  <% en_url = request.path + "?" + request.query_parameters.merge(lang: 'en').to_query %>
  <% fr_url = request.path + "?" + request.query_parameters.merge(lang: 'fr').to_query %>
  <% terms_label = current_lang == 'fr' ? 'Conditions de service' : 'Terms of Service' %>
  <% menu_toggle_label = current_lang == 'fr' ? 'Afficher ou masquer le menu de navigation' : 'Toggle navigation menu' %>

  <div class="topbar-utility">
    <div class="canadiana-shell topbar-utility-inner">
      <ul class="topbar-utility-list">
        <li class="topbar-language-switch">
          <% if current_lang == 'fr' %>
            <%= link_to terms_label, terms_of_service_path(lang: current_lang) %> | <%= link_to 'EN', en_url, lang: 'en' %> | <span aria-current="true" lang="fr">FR</span>
          <% else %>
            <%= link_to terms_label, terms_of_service_path(lang: current_lang) %> | <span aria-current="true" lang="en">EN</span> | <%= link_to 'FR', fr_url, lang: 'fr' %>
          <% end %>
        </li>
        <li class="topbar-socials">
          <a class="sm-icon bluesky" href="https://bsky.app/profile/crkn-rcdr.bsky.social" target="_blank" rel="noopener noreferrer" aria-label="Bluesky">
            <span class="visually-hidden">Bluesky</span>
          </a>
          <a class="sm-icon facebook" href="https://www.facebook.com/CRKNRCDR/" target="_blank" rel="noopener noreferrer" aria-label="Facebook">
            <span class="visually-hidden">Facebook</span>
          </a>
          <a class="sm-icon linkedin" href="https://www.linkedin.com/company/canadian-research-knowledge-network" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">
            <span class="visually-hidden">LinkedIn</span>
          </a>
          <a class="sm-icon instagram" href="https://www.instagram.com/crkn.rcdr/" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
            <i class="bi bi-instagram" aria-hidden="true"></i>
            <span class="visually-hidden">Instagram</span>
          </a>
        </li>
      </ul>
    </div>
  </div>

  <div class="topbar-main">
    <div class="canadiana-shell topbar-main-inner">
      <%= link_to root_path(lang: current_lang), class: 'navbar-brand navbar-logo', aria: { label: application_name } do %>
        <span class="visually-hidden"><%= application_name %></span>
      <% end %>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-bs-toggle="collapse" data-bs-target="#user-util-collapse" aria-controls="user-util-collapse" aria-expanded="false" aria-label="<%= menu_toggle_label %>">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-lg-end topbar-main-collapse" id="user-util-collapse">
        <%= render 'shared/user_util_links' %>
      </div>
    </div>
  </div>
</nav>
